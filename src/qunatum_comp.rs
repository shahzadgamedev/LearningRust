use std::io;

fn simulate_quantum_computer()
{
    println!("Welcomeee... : Enter number of Qubits .. ");
    let mut guess: String = String::new();

    io::stdin()
        .read_line(&mut guess)
        .expect("You Darn Failed Lol to Maintain Singularity!!!");
}